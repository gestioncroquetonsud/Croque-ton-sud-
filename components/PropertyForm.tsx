'use client'
import {FormEvent,useEffect,useState} from 'react';import Modal from './Modal';import {createClient} from '@/lib/supabase/client'
export default function PropertyForm({onClose,onSaved}:{onClose:()=>void,onSaved:()=>void}){const [clients,setClients]=useState<any[]>([]),[cities,setCities]=useState<any[]>([]),[error,setError]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{const s=createClient();Promise.all([s.from('clients').select('id,company_name').eq('status','active').order('company_name'),s.from('cities').select('id,name').eq('active',true).order('name')]).then(([a,b])=>{setClients(a.data||[]);setCities(b.data||[])})},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),s=createClient();const {data:p,error:pe}=await s.from('properties').insert({client_id:f.get('client_id'),city_id:f.get('city_id'),label:f.get('label'),internal_ref:f.get('internal_ref')||null,address:f.get('address')||null}).select('id').single();if(pe||!p){setError(pe?.message||'Erreur');setBusy(false);return}const {error:qe}=await s.from('qr_codes').insert({property_id:p.id,status:'active',monthly_threshold:50});if(qe){setError('Logement créé, mais erreur QR : '+qe.message);setBusy(false);return}onSaved();onClose()}return <Modal title="Ajouter un logement" onClose={onClose}><form className="form" onSubmit={submit}><label>Conciergerie*<select name="client_id" required><option value="">Sélectionner…</option>{clients.map(x=><option key={x.id} value={x.id}>{x.company_name}</option>)}</select></label><div className="form2"><label>Nom / référence logement*<input name="label" required placeholder="Appartement Port de Toulon"/></label><label>Référence interne<input name="internal_ref"/></label></div><label>Adresse<input name="address"/></label><label>Ville*<select name="city_id" required><option value="">Sélectionner…</option>{cities.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>{error&&<p className="formerror">{error}</p>}<div className="formactions"><button type="button" onClick={onClose}>Annuler</button><button className="primary" disabled={busy}>{busy?'Création…':'Créer logement + QR'}</button></div></form></Modal>}
