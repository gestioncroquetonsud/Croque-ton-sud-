import Shell from './Shell';export default function ListPage({title,subtitle,button,columns,empty}:{title:string,subtitle:string,button:string,columns:string[],empty:string}){return <Shell title={title} subtitle={subtitle} action={<button className="primary">+ {button}</button>}><div className="panel tablepanel"><div className="toolbar"><input placeholder="Rechercher…"/><button>Filtres</button></div><div className="table"><div className="tr th">{columns.map(c=><span key={c}>{c}</span>)}</div><div className="tableempty"><b>{empty}</b><span>Les données apparaîtront ici dès leur création.</span></div></div></div></Shell>}
