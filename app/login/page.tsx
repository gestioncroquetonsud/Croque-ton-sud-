'use client'
import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client'
const ADMIN=process.env.NEXT_PUBLIC_ADMIN_EMAIL||'gestion.croquetonsud@gmail.com'
export default function Login(){const router=useRouter(),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget),email=String(f.get('email')||'').trim().toLowerCase();if(email!==ADMIN.toLowerCase()){setError('Cette adresse n’est pas autorisée à accéder au BackOffice.');setBusy(false);return}const {error}=await createClient().auth.signInWithPassword({email,password:String(f.get('password')||'')});if(error){setError('Connexion impossible. Vérifiez votre adresse et votre mot de passe.');setBusy(false);return}router.replace('/dashboard');router.refresh()}return <div className="login"><div className="loginart"><img src="/brand/logo-full.jpeg" alt="Croque ton Sud"/><h1>Votre Sud, piloté simplement.</h1><p>Conciergeries, logements, QR permanents, guides PDF, alertes et statistiques dans un seul BackOffice.</p><div className="palette"><i style={{background:'#FCD800'}}/><i style={{background:'#FF8DC9'}}/><i style={{background:'#FE5129'}}/><i style={{background:'#1A82F5'}}/><i style={{background:'#84CAFE'}}/><i style={{background:'#01C464'}}/><i style={{background:'#821BFE'}}/></div></div><form className="loginbox" onSubmit={submit}><div className="mobilebrand"><img src="/brand/logo-icon.jpeg" alt="Croque ton Sud"/></div><h2>Connexion administrateur</h2><p>Accès privé Croque ton Sud</p><label>Adresse email<input name="email" type="email" defaultValue={ADMIN} required/></label><label>Mot de passe<input name="password" type="password" required/></label>{error&&<p className="formerror">{error}</p>}<button disabled={busy}>{busy?'Connexion…':'Se connecter'}</button><small>Accès réservé à {ADMIN}</small></form></div>}
